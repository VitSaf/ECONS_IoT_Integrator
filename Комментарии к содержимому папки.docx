Все файлы кроме main.py и mes_data.py автогенерированные на основе iiot_common.proto и iiot_device.proto, которые были получены от разработчиков — это один из принципов общения клиента с сервером на основе протокола gRPC.
Документация на библиотеки gRPC протокола и сам протокол: https://grpc.io/docs/languages/python/basics/

Файл main.py – основной. В нём описаны все взаимодействия, и в нём же происходит вызов функций.
Файл mes_data.py – содержит класса MesData, хранящий инкапсулированные данные. В  нём же определен вид результирующего xml файла (toXML()).
